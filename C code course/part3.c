#include <stdio.h>
main ()
{

    //datatype name ; >> this is  declaration
    int data1, data2, data3, data4;

    // there is 32 keywords that you cant register it as data
    // open picture1.png there are the 32 keywords

    //for ex. you cant do this 

    // int while; this is wrong 
    // int hoxasfda ; this is correct


    // datatype name = value ; >> = value ; "= value" is  intialization
    int i=3, e=2, t=4;
    float f = 5.513;
    double d = 5.513; 
    char k = 'a';
    char w[] ="Hello world";
    // "[]" this array symbol to store not just one charter
 

    printf("the value of %d\n", e);
    printf("the value of %f\n", f);
    printf("the value is %.2f\n", f);
    printf("the value is %.3f\n", d);
    printf("the value is %c\n", k);
    printf("the value is %s\n", w);
    
    // "%s" for strings not only one char
    // "%f" is to set the type of the value that will be printed
    // ".2" to print only 2 numbers after the semicolon
    // "%d" to print integer numbers
    // "%f" to print floating numbers & double numbers
   //  "%c" to print character 

   // go to ex. program1
}